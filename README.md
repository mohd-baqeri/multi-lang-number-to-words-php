# MultiLangNumberToWords (Persian | Dari and English)
MultiLangNumberToWord is a library to convert numbers to words

## How to use
1. download the library
  
2. import the numberToWord class in your project and create an object from the class

3. call numberToWords(myNumber, language) function to convert numbers to words (there is an example inside the index.php file)
